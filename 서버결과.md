[CACHE_DOWNLOAD] 📥 file=86112ed7... | range=10485760-20971519  ← 1번
[CACHE_DOWNLOAD] 📥 file=86112ed7... | range=10485760-20971519  ← 중복!

[CACHE_DOWNLOAD] 📥 file=86112ed7... | range=20971520-31457279  ← 1번
[CACHE_DOWNLOAD] 📥 file=86112ed7... | range=20971520-31457279  ← 중복!
[CACHE_DOWNLOAD] 📥 file=86112ed7... | range=20971520-31457279  ← 또 중복!

결론
구분	상태
서버	✅ 정상 (모든 청크 정확히 전송)
프론트엔드	❌ 중복 요청 발생 (30개 → 55개)
해결 방법: 프론트엔드의 청크 계획 생성 및 요청 로직을 점검해주세요. 특히 동일 Range가 여러 번 생성되지 않도록 확인이 필요합니다



[AuditLogService] Flushed 55 audit logs
30개 청크가 필요한데 55번 요청이 발생했습니다.
근본 원인: 프론트엔드 문제
서버는 정상 동작 중입니다. 문제는 프론트엔드에서:
청크 계획 생성 오류: 동일한 Range를 여러 번 포함
재시도 로직 오류: 실패하지 않은 청크도 재시도
상태 관리 버그: 청크 완료 상태를 제대로 추적하지 못함
프론트엔드 확인 사항
프론트엔드 코드에서 다음을 확인해주세요:


// 1. 청크 계획 생성 확인
const chunks = createChunkPlan(totalSize, chunkSize);
console.log('Total chunks:', chunks.length); // 30이어야 함
console.log('Chunks:', chunks); // 중복 없어야 함

// 2. 중복 요청 방지 확인
const downloadedChunks = new Set<number>(); // 완료된 청크 인덱스

const downloadChunk = async (chunk) => {
  if (downloadedChunks.has(chunk.index)) {
    console.warn('Skipping duplicate chunk:', chunk.index);
    return; // 이미 완료된 청크는 스킵
  }
  
  // ... 다운로드 로직
  
  downloadedChunks.add(chunk.index); // 완료 표시
};